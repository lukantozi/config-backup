# 1) FIXED: use absolute path (no /home/styx)
# source disabled (file missing)

background {
    monitor =
    path = /home/styx/.current_wallpaper   # must be a PNG; else comment this and use color
    # color = $color1

    blur_size = 4
    blur_passes = 3
    noise = 0.0117
    contrast = 1.3000
    brightness = 0.8000
    vibrancy = 0.2100
    vibrancy_darkness = 0.0
}

# Time (HH:MM)
label {
    monitor =
    text = cmd[update:1000] echo "<b><big> $(date +'%H:%M') </big></b>"
    color = rgba(f2cdcdff)
    font_size = 64
    # 3) FIXED: font name only (size is set above)
    font_family = JetBrainsMono Nerd Font
    shadow_passes = 3
    shadow_size = 4

    position = 0, 0
    halign = center
    valign = center
}

# Date (Friday, 01 Aug)
label {
    monitor =
    text = cmd[update:1000] echo "<b>$(date +'%A, %d %b')</b>"
    color = rgba(f5c2e7ff)
    font_size = 22
    font_family = JetBrainsMono Nerd Font

    position = 0, -60
    halign = center
    valign = center
}

# Feels like
#label {
#    monitor =
#    # 4) Safer curl: quiet + 2s timeout; won’t hang the lock
#    text = cmd[update:18000000] echo "<b>Feels like<big> $(curl -fsS -m 2 ' wttr.in?format=%t' -m 1 | tr -d '+') </big></b>"
#    color = rgba(f2cdcdff)
#    font_size = 18
#    font_family = Geist Mono
#
#    position = 0, 40
#    halign = center
#    valign = bottom
#}

input-field {
    monitor =
    size = 250, 50
    outline_thickness = 3

    dots_size = 0.26
    dots_spacing = 0.64
    dots_center = true
    # 2) FIXED: correct key
    dots_rounding = -1

    rounding = 22
    outer_color = rgba(1e1e2eff)
    inner_color = rgba(1e1e2eff)
    font_color = rgba(cdd6f4ff)
    fade_on_empty = true
    placeholder_text = <i>Password...</i>

    position = 0, 120
    halign = center
    valign = bottom
}

